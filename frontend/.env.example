# === Frontend Configuration ===
# Base URL for the backend API; used by all frontend API calls.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# === Supabase Configuration ===
# Get these credentials from your Supabase project dashboard:
# Project Settings → API → Project URL and anon public key
# Supabase project URL for direct database access
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# Supabase anon/public key (safe to expose in frontend)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
